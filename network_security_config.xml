<?xml version="1.0" encoding="utf-8"?>
<!-- network_security_config.xml
     Colocar en raiz del proyecto.
     Referenciado en buildozer.spec con: android.network_security_config
     
     Esta configuracion:
     - Bloquea trafico HTTP en texto plano (solo HTTPS en produccion)
     - Permite HTTP solo en emulador para debug local
     - Requerido por Google Play desde Android 9+
     - Equivalente en iOS: App Transport Security (automatico con Kivy)
-->
<network-security-config>

    <!-- Produccion: solo HTTPS para el dominio de Supabase y la API -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">supabase.co</domain>
        <domain includeSubdomains="true">jurislex.com</domain>
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </domain-config>

    <!-- Desarrollo: permite HTTP solo en localhost y emulador -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!-- Configuracion base: bloquear HTTP por defecto -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

</network-security-config>
